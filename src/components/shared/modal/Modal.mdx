## Modal

This component renders an overlay with default close button, header and body text.

### Props

- `children`: modal body content to render
- `dismissFn`: function that gets triggered when close button is pressed
- `isOpen`: controls whether modal is visible or not
- `title`: modal heading
