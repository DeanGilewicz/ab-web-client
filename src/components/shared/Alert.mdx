## Alert

This component renders an overlay that "slides in" at the bottom right of the viewport

### Props

- `level`: alert level
- `message`: text user will see
- `onDismiss`: triggers behavior when alert close button is pressed
- `open`: controls whether alert is open or not
